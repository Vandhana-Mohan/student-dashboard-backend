const { faker } = require('@faker-js/faker');

const firstName = faker.person.firstName(); 
const lastName = faker.person.lastName();
const Email = faker.internet.email(); 
const skill = faker.person.jobTitle()

for(let i = 0; i < 10; i++){

}